# 项目名称

这是一个基于C语言的计费管理系统项目。

## 项目架构

### 整体结构
本项目采用模块化设计，将代码分为多个功能模块，便于维护和扩展。项目结构如下：

```
wut_workbench/
├── makefile          # 构建脚本，用于编译项目
├── README            # 项目说明文档
├── build/            # 构建输出目录
│   └── main          # 编译生成的可执行文件
├── csrc/             # 源代码目录
│   ├── main.c        # 主程序入口，程序启动点
│   ├── menu.c        # 菜单系统，处理用户界面和交互
│   ├── service.c     # 服务层，提供核心业务逻辑
│   ├── tool.c        # 工具模块，提供通用工具函数
│   ├── billing_file.c # 计费文件操作模块
│   ├── billing_service.c # 计费服务模块
│   ├── card_file.c   # 卡片文件操作模块
│   └── card_service.c # 卡片服务模块
|   └── money_service.c       # 货币模块，处理货币计算
|   └── money_file.c         # 货币文件操作模块
|   └── rate_file.c         # 费率文件操作模块
|   └── rate_service.c       # 费率服务模块
├── include/          # 头文件目录
│   ├── global.h      # 全局定义和宏
│   ├── model.h       # 数据模型定义
│   ├── menu.h        # 菜单相关头文件
│   ├── service.h     # 服务层头文件
│   ├── tool.h        # 工具模块头文件
│   ├── billing_file.h # 计费文件头文件
│   ├── billing_service.h # 计费服务头文件
│   ├── card_file.h   # 卡片文件头文件
│   └── card_service.h # 卡片服务头文件
|   └── money_service.h       # 货币服务头文件
|   └── money_file.h         # 货币文件头文件
|   └── rate_file.h         # 费率文件头文件
|   └── rate_service.h       # 费率服务头文件
├── data/             # 数据文件目录
│   ├── biling.ams    # 计费数据文件
│   ├── card.ams      # 卡片数据文件
│   ├── rate.ams      # 费率数据文件
│   
└── tests/            # 测试目录（目前为空）
```

### 模块说明

#### 核心模块
- **main.c**: 程序的入口点，负责初始化系统并启动主循环。
- **menu.c**: 实现用户界面菜单，提供命令行交互功能。
- **service.c**: 核心业务逻辑层，协调各个子模块的工作。

#### 功能模块
- **计费模块** (billing_*.c/h): 处理计费相关的文件操作和服务逻辑。
- **卡片模块** (card_*.c/h): 管理卡片信息的文件操作和服务。

#### 数据层
- **data/**: 存储应用程序运行所需的数据文件，包括计费记录、卡片信息、费率设置等。

### 构建和运行
1. 使用makefile编译项目：
   ```
   make
   ```
2. 运行可执行文件：
   ```
   ./build/main
   ```
3. 直接运行:
    ```
    cd wut_workbench
    make run
    ```
### 依赖
- GCC编译器
- Linux环境 Ubuntu 22.04

### 开发环境
- 操作系统：Linux Ubuntu 22.04
- 编程语言：C makefile
- 构建工具：make
